# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# macOS
.DS_Store

# Claude Code session-local files
.claude/

# spikes/out and testdata are intentionally tracked (Phase 0 artifacts / fixtures)

# Spike-generated audio (spikes/audio_playhead.py recreates on demand)
spikes/out/click.wav
spikes/out/click.mp3

# M5 spike intermediates (spikes/system_breaks.py rewrites these on demand)
spikes/out/_brk_*.musicxml

# scratch staging area, never part of the tree
_to_delete/
